{% extends 'base.html.twig' %}

{% block title %}LigneStatut{% endblock %}

{% block body %}
    <h1>LigneStatut</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligne_statut.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ ligne_statut.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ligne_statut_index') }}">retour</a>

    <a href="{{ path('ligne_statut_edit', {'id': ligne_statut.id}) }}">modification</a>

    {{ include('ligne_statut/_delete_form.html.twig') }}
{% endblock %}

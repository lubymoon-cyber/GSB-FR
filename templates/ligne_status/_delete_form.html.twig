<form method="post" action="{{ path('ligne_status_delete', {'id': ligne_status.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ligne_status.id) }}">
    <button class="btn">Delete</button>
</form>
